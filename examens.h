#ifndef EXAMENS_H
#define EXAMENS_H

#include "types.h"  

//int compareDuree(const void *a, const void *b);
//void freeExamens(Examen *examens);
void planifierExamens(const char* nomPromo);
void saisirModalitesExamens(ModalitesExamens *modalites);

#endif // EXAMENS_H

